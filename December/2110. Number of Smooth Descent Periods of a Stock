class Solution {
    public long getDescentPeriods(int[] prices) {
        long count = 1;
        long curr = 1;

        for (int i = 1; i < prices.length; i++) {
            if (prices[i - 1] - prices[i] == 1) {
                curr++;
            } else {
                curr = 1;
            }
            count += curr;
        }
        return count;
    }
}
